# Virtual env
.venv/
venv/

# PyInstaller
build/
dist/
*.spec.bak

# Runtime
logs/
state.json
__pycache__/
*.pyc
*.pyo
.DS_Store

# Credentials (never commit)
credentials.json
*.properties
.env

# Sync cache
sync/leg_cache.json
data.backup.json

# Wrangler local dev cache (belongs in kairos/ portal, not here)
.wrangler/

# Agent runtime output — contains REAL brokerage holdings, never commit
data.json
# Local broker probe / dev scripts (not part of the shipped agent)
test_*.py
test-kl.js
